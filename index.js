document.addEventListener("DOMContentLoaded", function() {
  // we need to be able to get things on the page
  /*
    Objective: Get info from api and see current weather
    1. Get data -> Fetch
    2. Grab some JSON
    3. Where should it go on the page -> InnerHTML way
    4. Put on the page
  */

  let app = new App();
  // app.fetchWeather();
});
